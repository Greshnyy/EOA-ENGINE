#pragma once
#include <string>
#include <vector>
#include <memory>

namespace eoa {

// Заглушка для MaterialGraph - будет реализована позже
class MaterialGraph {
public:
    MaterialGraph() = default;
    ~MaterialGraph() = default;
};

} // namespace eoa
